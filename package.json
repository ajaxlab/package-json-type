{
  "name": "package-json-type",
  "version": "1.0.0",
  "description": "A TypeScript definition for the CommonJS package descriptor file.",
  "types": "types/index.d.ts",
  "scripts": {
    "build": "npm run lint && npm run test && npm run clean && tsc -b",
    "clean": "rimraf types",
    "dev": "tsc -b --watch",
    "lint": "tslint 'src/**/*.ts' 'test/**/*.ts'",
    "test": "ts-node test/package-json.spec.ts"
  },
  "keywords": [
    "package-json",
    "package",
    "typescript",
    "definition"
  ],
  "author": "ajaxlab7@gmail.com",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ajaxlab/package-json-type.git"
  },
  "bugs": {
    "url": "https://github.com/ajaxlab/package-json-type/issues"
  },
  "homepage": "https://github.com/ajaxlab/package-json-type",
  "devDependencies": {
    "rimraf": "^2.6.2",
    "ts-node": "^8.0.3",
    "tslint": "^5.12.1",
    "tslint-lines-between-class-members": "^1.3.1",
    "typescript": "^3.3.3"
  }
}
